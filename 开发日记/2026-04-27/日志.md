---
date: 2026-04-27
session_focus: L3 月报蓝本完成 + GitHub 初始化 + Handoff 到 L2 优化
next_focus: L2 重新优化
---

# 2026-04-27 开发日志

## 本次会话主轴

**完成 L3 月报蓝本(2026-03)v1 → v6 全迭代,沉淀 L3 需求原型,整体内容入 GitHub。**

## 完成事项

### 1. L3 月报 2026-03 v6 已交付

`~/Desktop/能源政策月报-2026-03.docx` (785 KB) + `.html` (1.27 MB)

**主要修正路径(每条都对应一类 L3 规则):**

| # | 错误 | 修法 |
|---|---|---|
| 1 | 决策卡片过多/无差异 | 收紧到 ⭐≥4 业务命中 4 篇,⭐5/⭐4 配色拆分 |
| 2 | 重点解读塞省级政策 | 改为 3 篇国家级新政深度,省级落地拆独立 §5 |
| 3 | 「本月省级落地」插在 §4.x 误导派生 | 拆 §5,narrative 强调"同源并行" |
| 4 | 山东 VPP「省级首批」事实错 | 改"第 2 个,前序:上海 SH_407 2025-06" |
| 5 | 「四大业务 + 乡村业务」业务表述错 | 强制三大业务 + 乡村单独标关注方向 |
| 6 | 品牌名「滴滴」对内不合适 | sanitize 改「公司」 |
| 7 | 「立即行动」过激紧迫感 | 改「趁早行动」(全文 + chart) |
| 8 | Word 弹「外部引用」提示 | TOC field → 静态段落 + InternalHyperlink + Bookmark |
| 9 | 数据健康卡 / 附录 C / Claude 尾注 | 全删 |
| 10 | 跨月演进图 + 2×2 图挤压相邻文字 | 图宽 700→540(超 6.27" A4 内容区会被 Word 挤压) |

### 2. L3 需求原型沉淀

`00 背景资料/策略-L3月报需求原型.md`(9 节)— 把上述教训提炼为 L3 月报规范契约,下次月报 / 后续 L3 产出可直接套用。

### 3. 双格式同源管道

```
prep_docx_data.py   →  _docx_data.json
                            ├──→  render_docx.js   →  *.docx
                            └──→  render_html.py   →  *.html

render_march_charts.py  →  charts/*.png  ←  共享
```

**修一处,两端同步**:所有 sanitize / fact-check 在 prep 落地。

### 4. GitHub 入库

- Repo: https://github.com/ZaynShao/energy-policy-analysis(private)
- 首次 commit: `init: L1 collection + L2 wiki v3 + L3 monthly report prototype`
- 含 README.md / .gitignore / 全 vault(111 MB / 2104 文件)

## 当前 vault 状态

| 层 | 内容 | 状态 |
|---|---|---|
| L1 | 289 政策入库 | ✅ |
| L2 schema | schema_v3.md + 9 派生脚本 | ✅ 跑通 |
| L2 实体 | 88 canonical / 264 aliases | ✅ |
| L2 关系 | 198 关系 (7 类型) | ✅ |
| L2 主题结晶 | V2G / 电力市场 / 充电基建 | ✅ 3 个 |
| L2 lint | 11 项,1 error / 37 warnings | ⚠️ 见下 |
| L3 月报 | 2026-03 v6 蓝本 | ✅ |
| L3 决策卡 | 仅嵌入月报,未独立 | ⏸ |
| L3 深度报告 | 未起步 | ⏸ |

## 下一会话:L2 重新优化(用户指令)

**起点:** vault 已入 GitHub,L3 月报蓝本已稳。L2 是承上启下的瓶颈层,优化 L2 直接提升 L3 产出质量。

**可能方向(由下次会话开头 brainstorm 收口,不预设):**

- **数据补全:** 38 篇 ⭐≥4 政策被 lint 标低质量(L1 step4 PDF 全文未抽);5 篇 ⭐5 PDF 二进制未文本化
- **schema 升级:** L2 linkage 字段修饰语 fact-check lint(本轮发现「省级首批」类修辞错混入);比较级论断 lint;业务表述 schema 固化
- **派生加深:** 多主题结晶页(储能/设备更新/加油综合/乡村);stance extraction 加深
- **canonical 扩面:** 264 aliases 还不够覆盖,Agent 输出经常用未注册别名
- **lint 强化:** 当前 1 error(慈溪市能源规划缺 date)+ 37 warnings(低质量采集)— 关掉 warnings 而非忽略
- **provenance 完整化:** 3-tier 哲学要求每条派生事实都可溯源,当前部分 entities/relations 无 provenance 标记

## 未解决 TODO(优先级)

| 优先级 | TODO |
|---|---|
| 高 | L3 月报「比较级论断」fact-check lint(山东首批错触发) |
| 高 | L2 linkage 字段修饰语 fact-check lint(派生层拦截) |
| 中 | L1+L2 业务表述 schema 固化(3 业务 + 乡村关注方向) |
| 中 | §6 舆论温度计章节重写(评论数据需 1-2 月发酵) |
| 中 | L1 step4 PDF 二进制全文递归抽取 |
| 中 | L1 高规格政策渠道补全(全国人大网 / gov.cn 顶层) |
| 低 | OpenClaw cron 部署(月度自动出月报) |
| 低 | `build_report.sh` 一条命令串 4 渲染脚本 |

## 文件足迹(本次新增/修改)

**新增:**
- `00 背景资料/策略-L3月报需求原型.md` ← L3 蓝本规范
- `_meta/scripts/render_html.py` ← HTML renderer
- `开发日记/2026-04-27/日志.md` ← 本日志
- `README.md` ← repo 入口
- `.gitignore`

**修改(本次会话累计):**
- `_meta/scripts/prep_docx_data.py` ← sanitize 4 类(三大业务/虚构业务线/品牌/趁早)+ §5 evolution_chains 改造
- `_meta/scripts/render_docx.js` ← 7 处事实修正 + §5 重写 + 图排版 + TOC 脱钩 + 删 §9/附录C
- `_meta/scripts/render_march_charts.py` ← 2×2 chart 文字「立即→趁早」

**输出:**
- `~/Desktop/能源政策月报-2026-03.docx` (785 KB)
- `~/Desktop/能源政策月报-2026-03.html` (1.27 MB)

---

## L2 cites_basis 抽取 + schema_v3 升级（A' 方案）

### 背景
- 上轮发现 supersedes 漏抓 96%、iterates 漏抓 67%（标题判定不够）
- 103 篇 ⭐≥4 政策（占 36%）开头带"根据 xxx 政策"引文段——是真演进信号
- `references.jsonl` 已有 135 条 regex 文号匹配可作种子

### 完成动作

**schema_v3.md（`_meta/schema_v3.md`）9 处修改：**
- §0 line 19：357→289
- §6 关系列表 7 类→8 类，加 `cites_basis` 行
- §6.2 加"详细见 §6.4"指引
- §6.3 新增：cites_basis 特殊性（与 references 关系 + 字段扩展 `location` / `semantic`）
- §6.4 新增：反链文件结构 `_index_by_policy/<id>.md` + 出向→入向命名表
- §11 派生管道：7 类→8 类，加 cites_basis 抽取行
- §13 v2→v3 对比表：7 种→8 种
- §14 不做边界：加 cites_basis 显式声明边界
- §14 line 472 顺手：357→289

**新增脚本 `_meta/scripts/extract_cites_basis.py`：**
- 3 阶段管道：位置过滤（800 字符开头）→ MiniMax-M2 LLM 语义判定 → 标题引用补漏
- commentary filter（标题含 `解读 / 报道 / 答记者问 / 转载` 跳过）
- env 驱动：`MINIMAX_API_KEY` / `MINIMAX_BASE_URL` / `MINIMAX_MODEL`
- 默认 model `MiniMax-M2`（plan key 经探测仅支持 M 系列）
- 含 ping 预热 + dry-run 模式

**新增数据 `1_extracted/relations/cites_basis.jsonl`：**
- **10 条 cites_basis**（7 LLM 判定 + 3 title_match）
- 核心场景命中：山东 VPP（P_2026_SD_03271ffc）→ NDRC 357（虚拟电厂指导意见）
- 4 条省级 → 国家级（吉林、四川、河南×2 → 国办/国家发改委/工信部文件）

**修存量 `1_extracted/relations/_summary_regex.md`：**
- "输入政策 357" → "289"

**新增 memory（global）：**
- `~/.claude/projects/-Users-shaoziyuan-code/memory/feedback_offer_options_with_global_context.md`
- 规则：A/B/C 选项前先列全局 TODO，让用户在大框架视野下选

### 关键发现（写下来供下次会话用）

1. **"省级首批" fact-check 不靠 cites_basis**——山东 VPP 政策本身没引用上海 407 号；"首批"检测靠 themes/ 时间线（同主题政策按时间排序），下一阶段做。
2. **vault 政策与解读混存**——`0_raw/policies/` 既装真政策也装"政策解读/答记者问/新闻报道"，导致 cites_basis 第一版抽到 3 条误抓（北京汽车以旧换新解读、电力领域答记者问、电力系统调节能力解读）。本次 commentary filter 缓解，但根因是 L1 分类问题——加进 8 TODO 候选。
3. **title_match 反向误抓**——征求意见稿与正式稿同名，title_match 简单子串匹会反向误抓（如 P_2023 征求意见稿 → P_2025 正式稿）。本次手动剔除 1 条，未来 v2 加 `to.date < from.date` 自动跳过。

### 下一步候选
- **B 阶段**：反链生成脚本（schema 已备好，一脚本到位 8 类入向）
- **L1 政策原文 vs 解读分类**（新发现的根因）
- **2_crystallized/themes/ 主题结晶**（目前空，是 fact-check"首批"的真依靠）

---

## L0 firecrawl 抓取残留清理（跨天到 2026-04-28）

### 背景
本次准备开 B 阶段时,用户指出"Base64-Image-Removed 无效链接还在",并要求处理共性问题。盘点发现 117 raw 文件含此污染、3730+ 行空 markdown 链接装饰图——firecrawl 抓政府网站时把页眉/页脚/打印按钮/扫码图都抓下来了,污染所有"读 raw 给 LLM"的下游派生(cites_basis / diffs / opinions / themes 结晶)。

### 完成动作

**新脚本 `_meta/scripts/clean_firecrawl_artifacts.py`:**
- 整行匹配 6 类模式(嵌入正文不动,避免误删):
  - A: `![](<Base64-Image-Removed>)` 图片占位
  - B: `[](<Base64-Image-Removed>)` 链接占位
  - C: `![](http(s)://*.png|jpg|gif|svg|ico|webp|bmp)` 政府网站装饰图
  - D: `[![](decoration)](url)` 装饰图嵌套链接
  - E: `[](http(s)://...)` 空 alt 链接
  - F: `![](data:image/...)` 内联 svg/base64
- 扫范围 `0_raw/{policies,commentaries,_duplicates}`,`rglob` 递归子目录

**战果:**
- policies/      162 文件 / 574 行
- commentaries/  178 文件 / 1203 行
- _duplicates/    38 文件 / 131 行
- **小计 378 文件 / 1880 行删除**
- 残留 12 文件 / 26 行(嵌入正文,行内有政策内容,安全规则保留)

**双 commit 安全策略:**
- Commit 1 (235f0ca) feat(L2) cites_basis 工作快照
- Commit 2 (2ae3ea7) chore(L0) firecrawl cleanup 单独 commit(回滚精准)

---

## L2 反链层 B 阶段:`_index_by_policy/` 全量生成

### 完成动作

**新脚本 `_meta/scripts/build_reverse_links.py`:**
- 读 8 类 typed relations (`<rel>.jsonl`) 全量
- 按 target_id group by → 生成 `_index_by_policy/<P_id>.md`
- 每个反链页:
  - yaml header: `policy_id` / `title` / `inbound_edge_count` / `last_updated` / `policy_file` (相对路径指回 0_raw)
  - body 8 个 section (按 schema_v3.md §6.4 命名表): `cited_as_basis_by` / `superseded_by` / `iterated_by` / `extended_by` / `clarified_by` / `referenced_by` / `aligns_with_by` / `conflicts_with_by`
  - section 内时间倒序(被引时间新的在上)
  - 条目格式简版 `- [[P_xxx]] — title (YYYY-MM-DD)`
- 全量重建模式(每次清空 _index_by_policy/ 旧 .md 后重生成)

**战果:**
- **44 反链页生成**(只为有入向边的 target 生成)
- 总入向边 210 = 8 类 jsonl 总和(双向对齐验证 ✓)
- 入度排行 Top 3:
  - P_2024_MOFCOM_392 (商务部以旧换新): 22 入向
  - P_2024_SC_7 (国务院设备更新行动方案): 21 入向
  - P_2023_GO_19_b (国办充电基建指导意见): 21 入向

### 关键发现:山东 fact-check 真相浮出

抽查 P_2025_NDRC_357_a (虚拟电厂指导意见) 反链页:
- 山东 VPP (2026-03-27) → NDRC 357 通过 `cites_basis`
- 上海 407 (2025-06-19) → NDRC 357 通过 `clarifies`
- **山东和上海不是直接 cites_basis 关系**,而是都汇集到 NDRC 357 这个国家级 hub

→ "省级首批"的伪命题不是靠 cites_basis 检测,而是靠 themes/虚拟电厂.md 时间线——这正是 P2 任务的价值所在。

### 下一步
- **P0-2 supersedes/iterates 用 cites_basis 反推重抽**(补 fact-check 最大盲区,supersedes 漏 96%)
- P1 themes/ 主题结晶 (碰到山东 fact-check 这种问题的真依靠)
- P1 cites_basis 二轮放宽 OPENING 800→1500

---

## 2026-04-28 P0-2 · supersedes/iterates v2 重抽

### 决策路径

接 04-27 留下的"supersedes 漏 96% / iterates 漏 67%"问题。问"做不做 + 怎么做"时拿了 4A:
1. **同时做** supersedes + iterates(同一份 prompt 三选一,边际成本几乎 0)
2. **多源融合**:cites_basis(10) + references(135) + 同 issuer 配对(21) → 去重 150 候选
3. **全部走 LLM**(Opus 主代理)
4. v2 文件抽查后覆盖正式

LLM 调用方式调整:不再调 MiniMax API(上一轮已能跑通,但 reasoning model 输出冗长 + token plan 受限),改成主代理拆 4 批扔给 4 个 general-purpose subagent 并发判定,主代理合并结果。

### Pipeline

#### Stage 1 候选生成 (`extract_evolution_v2.py`)

3 源融合,A.date > B.date 强约束:

```
ref:                117  (单 references)
same_issuer:         21  (同 issuer + ≤3y + 字符 jaccard ≥0.5)
basis,ref:            7
basis:                3
ref,same_issuer:      2
─────────────────────────
total:              150
```

参数:`ITERATES_TIME_WINDOW_YEARS=3` / `TITLE_JACCARD_MIN=0.5` / `KEEP_CONFIDENCE_MIN=0.7`

#### Stage 2 LLM 判定 (4 subagent 并发)

每个 subagent 收到 38/38/37/37 条候选,同一份判定 prompt:
- iterates:跨年度同名续作、同 issuer 同主题加力承接
- supersedes:evidence 含明确"废止/同时失效"
- neither:国家级 vs 地方级、细则 vs 主政策、同主题对齐

并发 ~60s 完成 150 判定(对比单线程 MiniMax 估计 5-10 min)。

#### Stage 3 写 v2 (`--load-judgments`)

```
total judgments:  150
kept:              13  (iterates=12, supersedes=1)
skipped:          137  (neither/低置信)
```

### 结果对比

|  | v1 | v2 |
|---|:-:|:-:|
| iterates | 1 (heuristic) | 12 (LLM) |
| supersedes | 1 (regex) | 2 |

v2 抓到的 12 iterates 几乎全是同 issuer 跨年同主题续作:
- MOFCOM 392 (2024) → MOFCOM 8_a (2025) → MOFCOM 467 (2026) 汽车以旧换新链
- NDRC 0725 加力 (2024) → NDRC 1745_c (2026) 设备更新链
- NDRC 0726 加力 (2024) → NDRC 13_b (2025) 加力扩围链
- NDRC 1656_a 同时废止 NDRC 889 + NDRC 1123_a (电力中长期市场基本规则更新)
- 重庆 / 北京 / 上海 各自跨年汽车换新链
- MEE 年度配额方案续作 (2020 → 2022)

### Carryover:1 条 v1 真值人工补回

**对照 v1 边查回归发现**:
1. ✗ `P_2025_NDRC_0912b2a5 → P_2025_NDRC_0106e7c2` (v1 iterates) — 不是真 iterates。  
   储能行动方案(2025-09)和电力调节能力优化方案(2025-01)同 issuer 但语义不同主题,v1 字符 jaccard heuristic 误判,v2 字符 jaccard 实际 <0.5 没抓到 = 正解。
2. ✓ `P_2025_NDRC_1656_a → P_2024_NDRC_1123_a` (v1 supersedes) — 真 supersedes。  
   evidence 段含明确"《电力中长期交易基本规则——绿色电力交易专章》(发改能源〔2024〕1123 号)同时废止"。但 v2 没抓到 — 因为 references stage 1 漏抓 1123 号(regex 文号格式不匹配)。**人工补回**到 supersedes.jsonl,`extracted_by=manual_carryover_v1`。

### 反链全量重建

```
44 → 53 反链页
210 → 222 入向边
```

新增 9 个 target 全是 v2 新捕获的 iterates 链中目标(老政策被新政策迭代 → 出现入向)。

### 已知 P1 问题(stage 1 漏抓)

`extract_doc_num.py` regex 对"发改能源〔2024〕1123号"格式存在漏匹配。修复后 references.jsonl 应能补齐这条边,但当前先用 manual_carryover 解决。下一轮做 references regex 升级时同步修。

### Commit 5: `2852660`

```
feat(L2): supersedes/iterates v2 重抽 — 多源融合 + LLM 判定
58 files changed, 679 insertions(+), 62 deletions(-)
```

### 下一步(剩余优先级)

- **P1 cites_basis 二轮**:OPENING 800 → 1500 字符放宽,预计补 5-10 条
- **P1 references regex 升级**:补"发改能源〔YYYY〕NNN号"等容错格式
- **P1 themes/ 主题结晶**:解锁山东 fact-check 类比较级论断
- **P2 regions/ 补全 + `_global_index.md`**
- **P3 daily/weekly lint** 上线 (含双向反链对齐)

---

## 2026-04-28 下半场 · L1 地基整改 + L2 themes 业务覆盖补全

### 决策路径(brainstorming)

P0-2 完成后,本来要直接做 themes 业务覆盖补全。但反思了一下:**先盘 L1 是否充分,免得做完 themes 发现池子薄要回头**。

起 Explore subagent 做 L1 audit,出报告:
- 数据完整性 ✅(289 篇正文 100%,99.6% gov.cn)
- 5 候选主题政策池 69-94 ✅ 全过 ≥10 阈值
- ❌ issuer 23 种"国发改"写法共存(P0-2 踩坑根因)
- ❌ registry 0 个 type=theme(89 总实体里没有)
- ⚠️ 政策 vs 解读边界:5% 误率(audit 抽 40 样本)
- 关于 frontmatter.theme / business_relevance 字段 audit 误报(schema §4 没要求)

修缮路线锁死:`P0a issuer 标准化 → P0b registry 加 theme → P1 顺手清理 → L2 5 主题结晶`

### Module 1 / P0a · issuer 标准化

写 `_meta/scripts/standardize_issuer.py`,加新字段 `issuer_canonical` 不动 `issuer`(可回滚)。

第一遍跑命中率 37%(仅 25 个 org 实体 / 51 alias 不够)。

加完善启发式:
- 拆联合发文("国家发展改革委、国家能源局" → 数组逐项)
- 去括号注释("唐山市人民政府(国家发改委转载)" 去注释)
- ndrc/nea 启发式(覆盖司局/办公厅/署名)
- 国家部委补丁(`mps`/`sat`/`sasac`/`cccp_central`/`pboc`/`mohurd`/`nfga`/`ncec`/`npc`/`scio`/`xinhua_media` 等)
- 省/市级动态 id(`drc_beijing` / `gov_chongqing` / `commerce_shanghai` / `miit_chongqing` / `mof_<region>` / etc)

第二遍命中率 **87.9%**(254/289 完全命中)。剩 35 篇含未命中 → review_queue,长尾低频(地级市 1 次出现)。

**P0-2 same_issuer 配对真正修复**:
- "国家发展改革委、国家能源局" (12 篇) 与 "国家发展和改革委员会、国家能源局" (11 篇) 现在统一识别为 `[ndrc, nea]`,跨年续作可正确配对

### Module 2 / P0b · registry 加 5 theme

直接 append 5 个 yaml block 到 `1_extracted/entities/registry.yaml`:
- `vpp_theme` (8 aliases)
- `energy_storage_theme` (10 aliases)
- `gas_station_transition_theme` (8 aliases)
- `equipment_renewal_theme` (9 aliases)
- `green_power_trading_theme` (9 aliases)

registry 实体数 89 → 94。

### Module 3 / P1 · 顺手清理(缩减版)

- 6 篇 region.level=未知 全部补全 ✓(5 国家级 + 1 市级)
- chinaenergyportal.org 0 篇(audit 报告报错)
- A 类(疑似解读)实际 **37 篇**(audit subagent 用 5% 抽样外推到 14 篇,实际 12.8%)
  - 严格规则筛 26 篇(明确"政策解读/答记者问/提案答复/署名文章/新闻报道")
  - 模糊 6 篇(转载/署名,可能是政策原文)
  - **决定推迟到下一轮**:全移会颠覆已生成 themes 政策池(_input.json 失效需重跑 5 个 subagent),scope 失控

### Module 4 / L2 · 5 主题结晶

`crystallize_theme.py` 跑 5 次产 timeline + regional-coverage + _input.json:

| theme | 政策 | 关系 | opinion 覆盖 |
|---|:-:|:-:|:-:|
| VPP_THEME | 73 | 42 | 22 (30%) |
| ENERGY_STORAGE_THEME | 79 | 26 | 21 (27%) |
| GAS_STATION_TRANSITION_THEME | 25 | 13 | **1 (4%)** ⚠️ |
| EQUIPMENT_RENEWAL_THEME | 84 | 126 | 11 (13%) |
| GREEN_POWER_TRADING_THEME | 45 | 25 | 15 (33%) |

5 个 general-purpose subagent 并发跑 overview.md + opinions-summary.md。**opinions strict 模式**严禁编造未抽 stance 政策的观点,必带"X/N (Y%) 覆盖率"标注 + 未覆盖政策清单。

加油站转型 opinion 覆盖仅 4%,subagent 转为"诚实数据缺口报告",标记 `data_sparsity: severe`,留待补采集深度评论后回填。

### 关键发现:opinions 数据链路全貌

为回答"opinions 怎么扩到 ≥40%",梳理了链路:
- 0_raw/commentaries/ (338 篇) → stance LLM 抽取 (5 agent batches, 248 stances) → aggregate_opinions.py → 1_extracted/opinions/ (54 政策, 18.7%)
- 抽样发现大量 stance 记录里 `stances: []` — 因 comment_type 是 reposted_original/news_report 而非真观点评论
- **18.7% 不是抽得不够,是 commentary 池质量问题**(转载新闻占大头)
- 扩到 ≥40% 需 L1 commentary 补采集深度评论源(公众号/行业媒体),独立工程

### 业务覆盖矩阵

```
充电业务: V2G ✓ + 充电基建 ✓ + 加油转型 ✓
电力业务: 电力市场 ✓ + V2G ✓ + 储能 ✓ + VPP ✓ + 绿电 ✓
加油业务: 加油站转型 ✓ + 以旧换新 ✓
横向:    以旧换新 ✓
```

**第一次** L3 月报"省级首批"类比较级论断有数据底座(VPP_THEME 提供山东 fact-check 时间线)。

### Commits

- `cd0c447` docs(spec): L1 地基整改 + L2 themes 业务覆盖补全 设计文档
- `32d6027` chore(L1): issuer 标准化 + 6 region 补全 (267 files / 1109 insertions)
- `4507022` feat(L2): registry.yaml 加 5 个 type=theme 实体
- `1a263cc` feat(L2): 5 主题结晶页 (30 files / 22593 insertions)

### 已知问题(留下一轮)

1. **A 类 26+6 篇政策解读混分类**:需要联合 commentaries 关联 (`related_policy`) 重建,scope 较大,独立批次做
2. **opinions ≥40% 真路径**:L1 commentary 池补采集深度评论源
3. **conflicts_with 抽取**:0 条边,L3 月报风险维度核心数据源
4. **references title_match 补漏**:cites_basis 已有此机制,搬到 references 是低成本翻倍密度
5. **supersedes regex 升级**:容错"〔YYYY〕XXX 号"格式,修 P0-2 漏 1 条 carryover 的根因
6. **_global_index.md**:schema 规定的顶层数据仪表盘尚缺
7. **diffs 补跑 13→22**:演进对比页质量
8. **issuer review_queue 35 篇低频长尾**:批量补 aliases 或 weekly lint 自动处理
